
		<br/>
		<p class="text-muted"><label>Displaying first 20 (or less).</label></p>
	


<div class="row">  	
	<% @results.each do |result| %>
	
	<div class="col-xs-6 col-sm-3 <%= cycle('result-even', 'result-odd') -%>">
		<h4 class="dropdown"><%= result.numbers %>
			<a class="dropdown-toggle caret lotto-num-menu" id="result-<%=result.id %>" data-toggle="dropdown" href="#"></a>
		</h4>
		<span class="text-muted"><%= result.game %><br/>
			<%= result.draw_date %>
		</span>
	</div>
	<% end %>
</div>